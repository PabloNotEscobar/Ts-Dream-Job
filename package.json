{
  "name": "online-courses",
  "version": "1.0.0",
  "private": true,
  "description": "Pixel-perfect course catalog with live search and category filters",
  "scripts": {
    "build": "sass src/scss/main.scss css/style.css --no-source-map --style=compressed",
    "watch": "sass --watch src/scss/main.scss css/style.css"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
